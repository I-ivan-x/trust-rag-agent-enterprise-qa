# Q5 Boundary F Addendum

This addendum records a versioned deterministic demonstration over the **frozen K0U
parser-uncovered 32-case scope**. It does not replace or rewrite the original Boundary F
artifacts in `data/q5_frontier/dev-k0u-audit`; those files remain byte-for-byte unchanged.

The addendum parser accepts only the closed runtime payload. It composes one observed status
predicate with two deontic action cues and adds a general `keep` / `keeps` / `keeping`
morphology for unchanged-state language. It cannot read Gold, authored policy IR, authoring
metadata, case identity, or evaluation grouping. Its source, function, complexity attestation,
frozen input artifacts, case set, output rows, and canonical artifact hashes are bound by the
addendum verifier.

Within the named frozen scope, recomputation produces 32 parsed decisions and 32 correct
decisions from 32 cases. Coverage is 1.0, conditional accuracy is 1.0, and conditional risk
within the **frozen K0U parser-uncovered 32-case scope** is 0.0. No model or external request is
used.

This is a frozen-scope deterministic demonstration, not evidence about open-world language
understanding or open-world LLM value. It does not establish that arbitrary policy prose can be
handled by the parser, and it does not authorize extrapolation beyond the frozen case set.

The mechanical disposition is `controlled_prose_track=closed`. K1 remains unapproved;
Boundary G and new K1 data are prohibited. This addendum intentionally does not synchronize
other reports or claim summaries before a later claim source of truth exists.
