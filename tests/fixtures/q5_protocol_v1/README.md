# Q5 protocol-v1 compact fixture

This is a frozen, one-case, three-system artifact set generated at `eaf2887`.
Git stores the text payloads in normalized form; the regression test restores
their legacy Windows line endings and the two zero-byte event ledgers before
verification. Never regenerate these sealed rows with the v2 grader.
