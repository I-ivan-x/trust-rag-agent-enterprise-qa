# Q5 protocol-v2 compact fixture

This committed mock artifact freezes the C1R protocol-v2 grader, metrics,
Gate, prompt, and report semantics. Tests materialize its normalized text with
the original CRLF bytes before verification. New executions must use v3.
