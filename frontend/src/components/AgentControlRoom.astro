---
import { Fingerprint, ShieldAlert } from "@lucide/astro";
import showcase from "../data/interview-showcase.json";
import Pipeline from "./Pipeline.astro";
import TrajectoryPlayer from "./TrajectoryPlayer.astro";
---

<section
  id="governed-runtime"
  class="section runtime-section"
  data-major-section
  data-section-name="Governed Runtime"
>
  <div class="wrap">
    <header class="section-heading heading-inline">
      <div>
        <p class="eyebrow">受治理运行时 · 虚构事故演示</p>
        <h2>请求 → 找证据 → 提议 → 权限检查 → 等待审批。</h2>
      </div>
      <div class="runtime-boundary">
        <Fingerprint size={18} aria-hidden="true" />
        <span>
          <strong>合成演示语料</strong>
          <small>{showcase.corpus_id} · 不参与正式评测</small>
        </span>
      </div>
    </header>

    <Pipeline />

    <div class="runtime-note">
      <ShieldAlert size={17} aria-hidden="true" />
      <p>当前手册和实时观察可以作为回滚依据；过期手册与无权限例外不能作为行动依据。</p>
    </div>

    <TrajectoryPlayer />
  </div>
</section>
